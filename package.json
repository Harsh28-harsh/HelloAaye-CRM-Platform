{
  "name": "helloaaye-assignment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix Client",
    "server": "npm run dev --prefix server",
    "both": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}